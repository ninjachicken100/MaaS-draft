apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No namePrefix or namespace override — namespaces are set explicitly in each file
resources:
  - maas-model.yaml
  - maas-subscription.yaml
  - rbac.yaml
